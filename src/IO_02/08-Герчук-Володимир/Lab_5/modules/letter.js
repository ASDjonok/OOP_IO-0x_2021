/**
 * A letter module. The lowest level.
 * @module letter
 */

/** Class representing a single letter. */
export default class Letter {
  /**
   * Create a letter.
   * @param {String} letter
   */
  constructor(letter) {
    this.letter = letter;
  }
}
