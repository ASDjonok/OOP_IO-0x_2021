/**
 * The punctuation marks module.
 * @module punctuation
 */
/** Class representing a punctuation mark. */
export default class Punctuation {
  /**
   * Create a single punctuation mark.
   * @param {String} punct
   */
  constructor(punct) {
    this.punctuation = punct;
  }
}
